# Web framework
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
pydantic-settings==2.2.1

# HTTP client (Ollama + backend çağrıları için)
httpx==0.27.0

# Logging & utilities
python-multipart==0.0.9
python-dotenv==1.0.1

# Production server
gunicorn==22.0.0


# DistilBERT intent classifier (Step 4.2)
# torch/transformers isteğe bağlı — yoksa rule-based fallback devreye girer.
# Render free plan 512 MB sınırı nedeniyle production'da yüklenmez.
# torch>=2.1.0
# transformers==4.40.0
# tokenizers==0.19.1
