# Web framework
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
pydantic-settings==2.2.1

# HTTP client (Ollama + backend çağrıları için)
httpx==0.27.0

# Logging & utilities
python-multipart==0.0.9
python-dotenv==1.0.1

# Production server
gunicorn==22.0.0

# Yerel HuggingFace / DistilBERT model inference (OPSİYONEL)
# torch CPU-only versiyonu ayrı index URL gerektirir; Dockerfile'da yönetilir.
# Yüklenmezse distilbert_classifier.py otomatik olarak rule-based fallback'e geçer.
# Docker build'de CUDA kütüphaneleri (~1.5 GB) indirilmesini önlemek için
# bu satırlar yorum satırı bırakılmıştır.
# torch==2.3.1+cpu      # CPU-only: Dockerfile'daki pip install adımında yükle
# transformers==4.45.0
# accelerate==0.30.0
# tokenizers==0.19.1
